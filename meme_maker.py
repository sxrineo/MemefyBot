from PIL import Image, ImageDraw, ImageFont


def make_noyes_meme(text: list):
    pass
    # width, height = 250, 250
    # font_title = ImageFont.truetype("georgiab.ttf", 32)
    #
    # with open(file="templates/noyes.jpg") as img:
    #     draw = ImageDraw.Draw(img)
    # #    draw.text((x, y), line, font=font_title, fill="black")
    #

def make_meme(template_name: str, text: list):
    pass
    # if template_name == "noyes.jpg":
    #     result = make_noyes_meme(text)
    # else:
    #     result = "Ошибка"
    #
    # return result
